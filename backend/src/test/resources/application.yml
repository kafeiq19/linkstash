spring:
  datasource:
    url: jdbc:sqlite:target/test-linkstash.db
    driver-class-name: org.sqlite.JDBC

linkstash:
  jwt:
    secret: test-secret-key-for-linkstash-jwt-hs256-32bytes!!
    expiration-days: 7
  cors:
    allowed-origins: http://localhost:5173
  metadata:
    timeout-ms: 500

logging:
  level:
    com.linkstash.backend: WARN
    org.springframework.security: WARN
